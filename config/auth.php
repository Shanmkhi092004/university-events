<?php
// Simple admin credentials - change these to secure values before production.
// For a more robust solution create a `users` table and store password hashes.

return [
    // admin username
    'username' => 'admin',
    // admin password (plain text for demo). Change to a stronger password.
    'password' => 'admin123'
];

?>
